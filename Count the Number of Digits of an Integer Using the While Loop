#include <stdio.h>
int main() 
{
    int num, rev, rem=0;
    printf("Input the number\n");
    scanf("%d", &num);
    int count = 0;
    while(num>0)
    {
        num/=10;
        count++;
    }
    printf("The number of digits in that integer is : %d", count);
    return 0;
}
